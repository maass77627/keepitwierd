post form

<%= form_for(@post) do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.label :content %>
    <%= f.text_field :content %>
   <br>
   <%= f.fields_for :vinyls do |vinyl| %>
    <%= vinyl.label :name %>
    <%= vinyl.text_field :name %><br>
 
    <%= vinyl.label :artist %>
    <%= vinyl.text_field :artist %><br>
 
    <%= vinyl.label :genre %>
    <%= vinyl.text_field :genre %><br>

    <%= vinyl.label :Profile_Image %>
    <%= vinyl.file_field :image, class: "form-control" %>
 
    <br>
   
    <%= f.submit %>
  <% end %>

  need to add input for vinyl image
  need to add input for vinyl.name

  need to add input for artist.name
  need to add input for genre.name

  Comment box 